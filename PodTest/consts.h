#ifndef CONSTS_H
#define CONSTS_H

#include <stdint.h>

struct Arena
{
    enum {
      WIDGTH = 9000,
      HEIGHT = 16000,
    };
};

struct ArenaScreen
{
    enum {
      WIDGTH = 600,
      HEIGHT = 1067,
    };
};

#endif // CONSTS_H
